# Style-Bert-VITS2 Configuration
# This file is auto-generated when not present.
# Most settings are configured via Gradio UI or CLI arguments.

# Current model name (updated by Gradio UI)
model_name: ""

# Custom dataset path (optional)
# If not specified, defaults to: {dataset_root}/{model_name}
# dataset_path: "your/dataset/path"

# Global defaults for scripts
device: cuda
num_processes: 4

# Training settings
train:
  spec_cache: true
  keep_ckpts: 3

# API Server settings (for server_fastapi.py)
server:
  port: 5000
  device: cuda
  language: JP
  limit: 100
  origins:
    - "*"
